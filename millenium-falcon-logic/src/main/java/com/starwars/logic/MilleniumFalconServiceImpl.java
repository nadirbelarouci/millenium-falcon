package com.starwars.logic;

import com.starwars.api.EscapePlan;
import com.starwars.api.InterceptedData;

class MilleniumFalconServiceImpl implements com.starwars.api.MilleniumFalconService {
  @Override
  public EscapePlan calculateEscapePlan(InterceptedData interceptedData) {
    return null;
  }
}
